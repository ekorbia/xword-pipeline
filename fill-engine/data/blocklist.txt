# blocklist.txt — words the fill engine will NEVER place, whatever their score.
#
# One word per line. Lines starting with '#' are comments; blank lines ignored.
# Case, spaces, and punctuation are ignored, so "EAT SLIP" == "eatslip" == EATSLIP.
#
# Use this for junk/unwanted entries — especially ones mis-scored HIGH in the
# wordlist, which the --keep-mean / --max-iffy quality gates cannot catch.
# Grow it over time from QA findings.

TOONIEBAR
EATSLIP
SHEREHITES
